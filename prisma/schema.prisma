generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Todas las entidades del SDD § 3
// (se implementan completas en TASK-02)
